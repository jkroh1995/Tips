##str.replace(oldchar, replace);
-문자열 내의 oldchar를 replace로 바꿔준다. 주로 특정 단어를 없앨 때 많이 쓴다.

##str.repeat(n);
-문자열을 n번 반복한다.

##Arrays.toString(arr);
String[] strArray = {"Hello", " ", "Java", " ", "Programming"};
String strArrayToString  = Arrays.toString(strArray);
