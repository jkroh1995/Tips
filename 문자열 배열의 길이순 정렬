//오름차순
Arrays.sort(tmp, Comparator.comparing(String::length));
//ex
{apple,car,blue} -> {car, blue, apple}


//내림차순
Arrays.sort(tmp, Comparator.comparing(String::length).reversed());
//ex
{apple,car,blue} -> {apple, blue, car}
